<UserControl xmlns="https://github.com/avaloniaui" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:vm="using:ListTracker.ViewModels" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:uc="clr-namespace:ListTracker.Views;assembly=ListTracker" mc:Ignorable="d" x:DataType="vm:TVShowsViewModel" x:Class="ListTracker.Views.TVShowsView" xmlns:view="using:ListTracker.Views">
    <DockPanel>
        <TabControl>
            <TabItem Header="All">
                <DockPanel>
                    <DataGrid Margin="1" Width="1000" ItemsSource="{Binding GridItemsBookmarked}" IsReadOnly="True" GridLinesVisibility="All" BorderThickness="2" BorderBrush="MediumPurple" SelectedItem="{Binding SelectedGridItem}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Binding="{Binding Index}" Header="{Binding GridCountItemsBookmarked}" Width="45" />
                            <DataGridTextColumn Binding="{Binding Title}" Header="Title" Width="*" />
                            <DataGridTextColumn Binding="{Binding Director}" Header="Director" Width="200" />
                            <DataGridTextColumn Binding="{Binding Year}" Header="Year" Width="70" />
                            <DataGridTextColumn Binding="{Binding Runtime}" Header="Runtime" Width="70" />
                            <DataGridTextColumn Binding="{Binding NumVotes}" Header="Votes" Width="100" />
                        </DataGrid.Columns>
                    </DataGrid>
                    <Button Command="{Binding OpenLink}" Margin="2">Open Link</Button>
                </DockPanel>
            </TabItem>
        </TabControl>
    </DockPanel>
</UserControl>